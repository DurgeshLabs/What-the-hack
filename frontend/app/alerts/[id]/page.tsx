"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { AlertCard, getAlertDetail, getSession } from "@/lib/api";
const colors: Record<string, string> = { low: "bg-emerald-100 text-emerald-800", medium: "bg-amber-100 text-amber-800", high: "bg-orange-100 text-orange-800", critical: "bg-red-100 text-red-800" };
export default function AlertDetail({ params }: { params: { id: string } }) { const [alert, setAlert] = useState<AlertCard | null>(null); const [error, setError] = useState<string | null>(null); const session = getSession(); useEffect(() => { if (session) getAlertDetail(session.access_token, params.id).then(setAlert).catch(e => setError(e.message)); }, [params.id, session?.access_token]); if (error) return <p className="rounded-lg bg-red-50 p-4 text-red-700">{error}</p>; if (!alert) return <p className="text-slate-500">Loading saved forecast…</p>; return <div className="space-y-6"><Link href="/alerts" className="text-sm font-semibold text-indigo-600">← Alerts</Link><section className="rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><div className="flex flex-wrap items-center gap-3"><span className={`rounded-full px-3 py-1 text-xs font-semibold ${colors[alert.severity]}`}>{alert.severity.toUpperCase()}</span><span className="text-sm text-slate-500">Saved {new Date(alert.created_at).toLocaleString()}</span></div><h2 className="mt-4 text-3xl font-semibold text-slate-900">{alert.title}</h2><p className="mt-2 text-slate-600">{alert.summary}</p><div className="mt-6 grid gap-4 sm:grid-cols-3"><Stat label="Peak risk" value={`${Math.round(alert.risk_score)}/100`}/><Stat label="MITRE stage" value={alert.predicted_attack_type ?? "Unknown"}/><Stat label="Model score" value={`${Math.round(alert.confidence_score * 100)}%`}/></div></section><section className="grid gap-6 lg:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="font-semibold text-slate-900">Why it was flagged</h3><div className="mt-4 space-y-3">{alert.top_feature_contributors.length ? alert.top_feature_contributors.map(item => <div key={item.feature}><div className="flex justify-between text-sm"><span>{item.feature.replaceAll("_", " ")}</span><span className="text-slate-500">{Math.round(item.contribution * 100)}%</span></div><div className="mt-1 h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-indigo-500" style={{width: `${Math.max(3, item.contribution * 100)}%`}}/></div></div>) : <p className="text-sm text-slate-500">No feature attribution was stored for this alert.</p>}</div></div><div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h3 className="font-semibold text-slate-900">Recommended next actions</h3><ol className="mt-4 space-y-3 text-sm text-slate-700">{alert.recommended_actions.map((action, i) => <li key={action} className="flex gap-3"><span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-indigo-50 font-semibold text-indigo-700">{i + 1}</span>{action}</li>)}</ol></div></section></div>; }
function Stat({label, value}: {label: string; value: string}) { return <div className="rounded-xl bg-slate-50 p-4"><p className="text-xs uppercase tracking-wide text-slate-500">{label}</p><p className="mt-1 font-semibold text-slate-900">{value}</p></div>; }
