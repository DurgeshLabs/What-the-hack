"use client";
import Link from "next/link";
import { useState } from "react";
import { IngestionJob, getJobStatus, getSession, startReplay } from "@/lib/api";
export default function UploadPage() { const [file, setFile] = useState<File | null>(null), [job, setJob] = useState<IngestionJob | null>(null), [error, setError] = useState<string | null>(null); const session = getSession(); async function upload() { if (!file || !session) return; setError(null); try { let current = await startReplay(session.access_token, file); setJob(current); const timer = window.setInterval(async () => { try { current = await getJobStatus(session.access_token, current.id); setJob(current); if (["completed", "failed"].includes(current.status)) { window.clearInterval(timer); if (current.status === "completed") localStorage.setItem("wth_source_id", current.traffic_source_id); } } catch (e) { window.clearInterval(timer); setError(e instanceof Error ? e.message : "Unable to check upload status"); } }, 1000); } catch (e) { setError(e instanceof Error ? e.message : "Upload failed"); } } if (!session) return <p className="text-slate-500">Sign in before uploading traffic.</p>; const finished = job?.status === "completed"; return <div className="mx-auto max-w-2xl space-y-6"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-indigo-600">Data intake</p><h2 className="mt-1 text-3xl font-semibold text-slate-900">Upload traffic replay</h2><p className="mt-2 text-sm text-slate-500">Use the normalized CSV contract: timestamp, source/destination, protocol, packets and bytes. The service builds 60-second windows automatically.</p></div><section className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><input type="file" accept=".csv,text/csv" onChange={e => setFile(e.target.files?.[0] ?? null)} className="block w-full rounded-lg border border-slate-200 p-3 text-sm"/><button onClick={upload} disabled={!file || !!job && !["completed", "failed"].includes(job.status)} className="mt-4 w-full rounded-lg bg-indigo-600 py-3 text-sm font-semibold text-white disabled:opacity-50">{job ? "Processing traffic…" : "Upload and build feature windows"}</button>{error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}{job && <div className="mt-5 rounded-lg bg-slate-50 p-4 text-sm"><p className="font-semibold capitalize text-slate-800">{job.status}</p><p className="mt-1 text-slate-600">Accepted {job.accepted_rows.toLocaleString()} of {job.total_rows.toLocaleString()} rows · skipped {job.skipped_rows.toLocaleString()}</p>{job.error_message && <p className="mt-2 text-red-700">{job.error_message}</p>}</div>}{finished && <Link href="/dashboard" className="mt-5 block rounded-lg border border-indigo-200 bg-indigo-50 p-3 text-center text-sm font-semibold text-indigo-700">Open your live dashboard →</Link>}</section></div>; }
